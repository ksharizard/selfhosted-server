services:
  profilarr:
    image: santiagosayshey/profilarr:v1.1.5
    container_name: profilarr
    volumes:
      - ./config:/config
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    restart: unless-stopped
    labels:
      tsbridge.enabled: true
      tsbridge.service.port: 6868
